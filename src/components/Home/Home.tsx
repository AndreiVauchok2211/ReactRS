export function Home() {
  return (
    <h2 style={{ margin: '10px', textAlign: 'center' }}>
      Redux is not configured, redirecting to the main route after submitting a
      form with data display is not configured. Form for non-controlled check is
      not implemented.
    </h2>
  );
}
